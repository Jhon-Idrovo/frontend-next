{
  "name": "learn-starter",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=development NODE_OPTIONS='--inspect --openssl-legacy-provider' next dev",
    "build": "NODE_OPTIONS='--openssl-legacy-provider' next build",
    "start": "export NODE_ENV=production && next start -p $PORT"
  },
  "dependencies": {
    "axios": "0.21.4",
    "chart.js": "^2.9.3",
    "next": "10.2.3",
    "react": "17.0.1",
    "react-chartjs-2": "2.11.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
